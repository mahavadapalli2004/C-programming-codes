#include<stdio.h>
main()
{
	char a[100],r[100];
	int i=0,end,count;
	printf("enter the any string");
	gets(a);
	while(a[count]!='\0'){
		count++;
	}
	end=count-1;
	for(i=0;i<count;i++)
	{
		r[i]=a[end];
		end--;
	}
	r[i]='\0';
	printf("\n %s",r);
	printf("\n %s",a);
}
